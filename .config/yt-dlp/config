# Save in ~/torrents
-o ~/torrents/%(title)s.%(ext)s

# Prefer 1080p or lower resolutions
-f bestvideo[height<=?720]+bestaudio/best

# Force MP4 container when merging (e.g., for videos with separate audio)
--merge-output-format mp4

# # Use this proxy
# --proxy 127.0.0.1:3128
